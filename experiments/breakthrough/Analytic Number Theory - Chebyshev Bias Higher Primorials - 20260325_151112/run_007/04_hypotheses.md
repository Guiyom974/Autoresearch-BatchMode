Based on the research problem provided, here are four testable hypotheses regarding the prime race between the residue classes $a=11$ and $a=1$ modulo 210.

### Hypothesis 1: The "Non-Residue Dominance" Persistence
*   **Hypothesis Statement:** The quadratic non-residue class ($a=11$) will maintain a strictly positive lead over the quadratic residue class ($a=1$) for the entirety of the search space $x \le 10^5$. That is, $\Delta(x) > 0$ for all primes $x \le 10^5$.
*   **Why it is testable:** This hypothesis makes a binary prediction (the crossover does not occur within the bound). It is falsifiable by the computational script: if the script encounters a state where $\Delta(x) \le 0$, the hypothesis is proven false.
*   **Experimental Test:** Run the discrete counting script up to $x = 10^5$. If the script completes without hitting the "Early Stopping" condition (the crossover), the hypothesis is supported.

### Hypothesis 2: Localized Maximum Bias Threshold
*   **Hypothesis Statement:** The maximum lead (Chebyshev bias) attained by the $a=11$ class before the first crossover point (or the end of the bound) will not exceed $\pi(10^3)$ in magnitude.
*   **Why it is testable:** This hypothesis provides a quantitative prediction about the magnitude of the prime distribution gap. It is testable because the script is required to track the running tally of $\Delta(x)$, which allows for the calculation of the maximum value of this delta.
*   **Experimental Test:** During the execution of the counting script, maintain a variable `max_delta` that updates whenever $\Delta(x)$ exceeds the current record. After the run, compare `max_delta` against the value of $\pi(10^3)$.

### Hypothesis 3: The "Early Crossover" Nullification
*   **Hypothesis Statement:** A crossover point ($\Delta(x) \le 0$) will occur within the initial micro-bound of $x \le 10^3$. 
*   **Why it is testable:** This creates a specific, narrow prediction about the *location* of the crossover, distinct from the broader $10^5$ limit. It tests the assertion that if a crossover exists, it is a highly localized, low-value phenomenon.
*   **Experimental Test:** Inspect the output logs of the script. If the script triggers an "Early Stopping" halt at any value of $x \le 10^3$, the hypothesis is confirmed. If the script passes $10^3$ without halting, the hypothesis is falsified.

### Hypothesis 4: Monotonicity of the Delta Function
*   **Hypothesis Statement:** The delta function $\Delta(x) = \pi(x; 210, 11) - \pi(x; 210, 1)$ will exhibit at least three distinct local minima before reaching $x = 10^5$.
*   **Why it is testable:** This hypothesis posits that the "race" is not a smooth, unidirectional march, but a volatile one with multiple "pull-backs" where the residue class ($a=1$) narrows the gap significantly, even if it does not overtake $a=11$.
*   **Experimental Test:** Analyze the data array generated by the script. Define a "local minimum" as a point where $\Delta(x_{i-1}) > \Delta(x_i) < \Delta(x_{i+1})$. Count the occurrences of these troughs in the recorded data to determine if the frequency of oscillations meets the threshold of three.