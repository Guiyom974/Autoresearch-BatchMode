"python Path(__file__).resolve() immutable root path construction best practices"
"python context manager for directory management nested paths"
"preventing FileNotFoundError python working directory drift nested temporary paths"